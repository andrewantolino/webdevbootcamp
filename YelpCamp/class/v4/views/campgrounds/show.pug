extends ../layout

block content
	h1= campground.name

	img(src=campground.image)
	p= campground.description

	p
		a(href="/campgrounds/"+campground._id+"/comments/new" class="btn btn-success") Add new comment
	each comment in campground.comments
		p
			strong= `${comment.author}`
			= ` - ${comment.text}`